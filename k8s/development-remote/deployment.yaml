apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev-project-kubequest-frontend-deployment
  namespace: dev-project-kubequest-frontend
  labels:
    app: dev-project-kubequest-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dev-project-kubequest-frontend
  template:
    metadata:
      labels:
        app: dev-project-kubequest-frontend
    spec:
      containers:
        - name: dev-project-kubequest-frontend
          image: corentin35/project-kubequest-frontend:develop-${TAG_VERSION}
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: '400m'
              memory: '512Mi'
            limits:
              cpu: '800m'
              memory: '1Gi'
      imagePullSecrets:
        - name: regcred
