apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dev-project-kubequest-frontend-ingress
  namespace: dev-project-kubequest-frontend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - dev.kubequest.flapi.org
      secretName: dev-project-kubequest-frontend-tls
  rules:
    - host: dev.kubequest.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: dev-project-kubequest-frontend-service
                port:
                  number: 3000
