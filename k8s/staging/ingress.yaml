apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: staging-project-kubequest-frontend-ingress
  namespace: staging-project-kubequest-frontend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - staging.kubequest.flapi.org
      secretName: staging-project-kubequest-frontend-tls
  rules:
    - host: staging.kubequest.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: staging-project-kubequest-frontend-service
                port:
                  number: 3000
