apiVersion: apps/v1
kind: Deployment
metadata:
  name: staging-project-kubequest-frontend-deployment
  namespace: staging-project-kubequest-frontend
  labels:
    app: staging-project-kubequest-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: staging-project-kubequest-frontend
  template:
    metadata:
      labels:
        app: staging-project-kubequest-frontend
    spec:
      containers:
        - name: staging-project-kubequest-frontend
          image: corentin35/project-kubequest-frontend:staging-${TAG_VERSION}
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: '400m'
              memory: '512Mi'
            limits:
              cpu: '800m'
              memory: '1Gi'
      imagePullSecrets:
        - name: regcred
