apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prod-project-kubequest-frontend-ingress
  namespace: prod-project-kubequest-frontend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - kubequest.flapi.org
      secretName: prod-project-kubequest-frontend-tls
  rules:
    - host: kubequest.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: prod-project-kubequest-frontend-service
                port:
                  number: 3000
